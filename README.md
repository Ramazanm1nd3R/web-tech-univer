# RomaCreditBank - Исправленный проект

## Что было исправлено

### Основные проблемы в старой структуре:
1. **Дублирование файлов** - React компоненты были в `/admin-panel/` и `/src/admin-panel/`
2. **Смешивание технологий** - статичные HTML файлы вперемешку с React
3. **Отсутствие системы сборки** - не было package.json в корне
4. **Путаница в роутинге** - непонятно где статика, где React

### Новая структура:

```
bank-project/
├── index.html              # Главная точка входа
├── package.json            # Зависимости проекта
├── vite.config.js          # Конфигурация Vite
├── src/
│   ├── main.jsx            # Точка входа React
│   ├── App.jsx             # Главный компонент с роутингом
│   ├── components/         # Переиспользуемые компоненты
│   │   └── AdminLayout.jsx
│   ├── pages/
│   │   ├── public/         # Публичные страницы
│   │   │   ├── HomePage.jsx
│   │   │   ├── ProductsPublicPage.jsx
│   │   │   └── RegisterPage.jsx
│   │   └── admin/          # Админские страницы
│   │       ├── LoginPage.jsx
│   │       ├── DashboardPage.jsx
│   │       ├── ProductsPage.jsx
│   │       └── UsersPage.jsx
│   ├── hooks/              # Кастомные хуки
│   │   └── useLocalStorageArray.js
│   ├── styles/             # Стили
│   │   └── global.css
│   └── assets/             # Статичные файлы
│       └── images/
```

## Установка и запуск

### 1. Установите зависимости:
```bash
npm install
```

### 2. Запустите dev сервер:
```bash
npm run dev
```

Проект откроется на `http://localhost:3000`

### 3. Для продакшн сборки:
```bash
npm run build
```

Скомпилированные файлы будут в папке `dist/`

## Роутинг

### Публичные страницы:
- `/` - Главная страница
- `/products` - Страница всех продуктов
- `/register` - Регистрация

### Админ панель:
- `/admin/login` - Логин (admin / 123456)
- `/admin` - Dashboard
- `/admin/products` - Управление продуктами
- `/admin/users` - Управление пользователями

## Ключевые изменения

### 1. Использование Vite вместо create-react-app
Vite быстрее и современнее. Моментальный HMR (Hot Module Replacement).

### 2. Единая точка входа
Все страницы теперь React компоненты, управляемые через React Router.

### 3. Общее хранилище
Продукты хранятся в localStorage под ключом `products` и используются как на публичных страницах, так и в админке.

### 4. Разделение ответственности
- `public/` - страницы для обычных пользователей
- `admin/` - админская панель с CRUD

### 5. Сохранены все функции
- ✅ Снежный эффект
- ✅ Таймер Black Friday
- ✅ Калькулятор депозита
- ✅ Корзина товаров
- ✅ CRUD продуктов в админке
- ✅ CRUD пользователей в админке
- ✅ Логи активности

## Технологический стек

- **React 18** - UI библиотека
- **React Router 6** - Роутинг
- **Vite** - Система сборки
- **localStorage** - Хранилище данных
- **CSS** - Оригинальные стили сохранены

## Советы по разработке

1. **Для добавления новой страницы:**
   - Создайте компонент в `src/pages/public/` или `src/pages/admin/`
   - Добавьте роут в `src/App.jsx`

2. **Для изменения стилей:**
   - Редактируйте `src/styles/global.css`

3. **Для работы с данными:**
   - Используйте хук `useLocalStorageArray` для синхронизации с localStorage

## Доступ к админке

**Логин:** admin  
**Пароль:** 123456

---

Проект теперь имеет чистую, понятную структуру и готов к дальнейшей разработке! 🚀
