# Миграция со старого проекта на новый

## Быстрый старт

### 1. Скачайте новый проект
Вся папка `bank-project` - это ваш новый проект.

### 2. Установите зависимости
```bash
cd bank-project
npm install
```

### 3. Запустите проект
```bash
npm run dev
```

Откроется `http://localhost:3000`

## Что изменилось

### ✅ Все работает из коробки
- Один `npm install` и `npm run dev`
- Не нужно запускать несколько проектов
- Все в одном месте

### ✅ Сохранены все функции
- Главная страница со снегом и таймером
- Калькулятор депозита
- Корзина товаров
- Админ панель (логин: admin / пароль: 123456)
- CRUD для продуктов и пользователей
- Все ваши стили

### ✅ Улучшена структура
```
Старый проект:
❌ 3 папки admin-panel
❌ Статичный HTML + React вместе
❌ 3 разных package.json
❌ Сломанный роутинг

Новый проект:
✅ Четкая структура
✅ Только React
✅ Один package.json
✅ Работающий роутинг
```

## Роуты

### Публичный сайт:
- `/` - Главная
- `/products` - Все продукты
- `/register` - Регистрация

### Админка:
- `/admin/login` - Вход (admin / 123456)
- `/admin` - Dashboard
- `/admin/products` - Управление продуктами
- `/admin/users` - Управление пользователями

## Если что-то не работает

### Проблема: Не устанавливаются зависимости
```bash
# Очистите кэш и попробуйте снова
rm -rf node_modules package-lock.json
npm install
```

### Проблема: Порт 3000 занят
Измените в `vite.config.js`:
```js
server: {
  port: 4000  // или любой другой
}
```

### Проблема: Не видны изображения
Убедитесь что папка `src/assets/images/` содержит:
- cardsjpeg.jpeg
- deposit.jpg
- ipoteka.jpeg
- invest.jpeg

## Следующие шаги

1. **Протестируйте все функции**
   - Откройте главную страницу
   - Добавьте товар в корзину
   - Войдите в админку
   - Создайте/измените продукт

2. **Кастомизируйте**
   - Измените стили в `src/styles/global.css`
   - Добавьте новые страницы
   - Настройте под свои нужды

3. **Задеплойте**
   ```bash
   npm run build
   # Папка dist/ готова для деплоя
   ```

## Полезные команды

```bash
# Разработка
npm run dev

# Сборка для продакшн
npm run build

# Предпросмотр продакшн сборки
npm run preview
```

## Поддержка

Если возникнут вопросы:
1. Проверьте README.md
2. Изучите PROBLEMS_ANALYSIS.md
3. Посмотрите код - он теперь хорошо структурирован

---

Удачи с проектом! 🚀
